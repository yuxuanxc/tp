package seedu.address.testutil;

import seedu.address.commons.core.index.Index;

/**
 * A utility class containing a list of {@code Index} objects to be used in tests.
 */
public class TypicalIndexes {
    public static final Index INDEX_FIRST_ATTRACTION = Index.fromOneBased(1);
    public static final Index INDEX_SECOND_ATTRACTION = Index.fromOneBased(2);
    public static final Index INDEX_THIRD_ATTRACTION = Index.fromOneBased(3);
}
